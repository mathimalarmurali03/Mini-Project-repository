# Mini-Project-repository
FPGA-based implementation of a Pipelined LZW (PLZW) compression algorithm using Verilog. The project improves the throughput of the traditional LZW algorithm by introducing pipelined stages for hash computation, dictionary lookup, and dictionary update, making it suitable for real-time edge data compression on FPGA platforms.
